<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gabriel da Silva Lima | Currículo</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Playfair+Display:wght@400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary-color: #333333; /* Preto para um visual minimalista */
            --primary-light: #555555;
            --secondary-color: #222222;
            --accent-color: #777777;
            --dark-color: #111111;
            --light-color: #f5f5f7;
            --text-color: #444444;
            --text-light: #777777;
            --white: #ffffff;
            --black: #000000;
            
            --font-main: 'Playfair Display', serif;
            --font-secondary: 'Poppins', sans-serif;
            
            --transition: all 0.3s ease;
            --shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
            --shadow-hover: 0 15px 40px rgba(0, 0, 0, 0.12);
            --border-radius: 20px;
            --glass-effect: backdrop-filter: blur(12px) saturate(180%);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
            scroll-padding-top: 80px;
        }

        body {
            font-family: var(--font-secondary);
            color: var(--text-color);
            background-color: var(--light-color);
            line-height: 1.6;
            overflow-x: hidden;
        }

        h1, h2, h3, h4, h5, h6 {
            font-family: var(--font-main);
            font-weight: 600;
            line-height: 1.3;
            color: var(--dark-color);
        }

        /* Efeito de gradiente animado suave */
        .gradient-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -2;
            background: linear-gradient(-45deg, #f5f7fa, #f0f0f0, #e8e8e8, #e0e0e0);
            background-size: 400% 400%;
            animation: gradient 25s ease infinite;
        }

        @keyframes gradient {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        /* Navbar flutuante elegante */
        .floating-nav {
            position: fixed;
            right: 30px;
            top: 50%;
            transform: translateY(-50%);
            z-index: 1000;
            background-color: rgba(255, 255, 255, 0.96);
            padding: 20px 12px;
            border-radius: 50px;
            box-shadow: var(--shadow);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.4);
        }

        .floating-nav ul {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .floating-nav li a {
            width: 48px;
            height: 48px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--text-light);
            font-size: 1.1rem;
            transition: var(--transition);
            position: relative;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
        }

        .floating-nav li a::before {
            content: '';
            position: absolute;
            width: 100%;
            height: 100%;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
            z-index: -1;
            transform: scale(0);
            transition: var(--transition);
            opacity: 0;
        }

        .floating-nav li a:hover, 
        .floating-nav li a.active {
            color: var(--white);
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(51, 51, 51, 0.2);
        }

        .floating-nav li a:hover::before,
        .floating-nav li a.active::before {
            transform: scale(1);
            opacity: 1;
        }

        /* Seção Hero moderna */
        .hero-section {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            padding: 100px 0;
            position: relative;
        }

        .hero-content {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 60px;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 40px;
        }

        .hero-text {
            flex: 1;
            animation: fadeInLeft 1s ease;
        }

        .greeting {
            font-size: 1.2rem;
            color: var(--primary-color);
            margin-bottom: 15px;
            font-weight: 600;
            display: inline-block;
            background-color: rgba(51, 51, 51, 0.1);
            padding: 10px 20px;
            border-radius: 50px;
            letter-spacing: 1px;
            text-transform: uppercase;
            font-family: var(--font-secondary);
        }

        .name {
            font-size: clamp(2.5rem, 6vw, 4rem);
            margin-bottom: 20px;
            color: var(--dark-color);
            line-height: 1.2;
            font-weight: 700;
        }

        .name span {
            color: var(--primary-color);
            position: relative;
            display: inline-block;
        }

        .name span::after {
            content: '';
            position: absolute;
            bottom: 8px;
            left: 0;
            width: 100%;
            height: 10px;
            background-color: rgba(51, 51, 51, 0.2);
            z-index: -1;
            border-radius: 8px;
        }

        .title {
            font-size: clamp(1.4rem, 3.5vw, 1.8rem);
            font-weight: 400;
            color: var(--text-light);
            margin-bottom: 30px;
            max-width: 600px;
            line-height: 1.5;
        }

        .hero-buttons {
            display: flex;
            gap: 20px;
            margin-top: 40px;
            flex-wrap: wrap;
        }

        .btn-main, .btn-secondary {
            padding: 16px 32px;
            border-radius: 50px;
            font-weight: 600;
            font-size: 1rem;
            transition: var(--transition);
            display: inline-flex;
            align-items: center;
            gap: 10px;
            position: relative;
            overflow: hidden;
            z-index: 1;
            letter-spacing: 0.5px;
        }

        .btn-main {
            background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
            color: var(--white);
            box-shadow: 0 15px 30px rgba(51, 51, 51, 0.2);
        }

        .btn-main:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 40px rgba(51, 51, 51, 0.3);
        }

        .btn-secondary {
            background-color: transparent;
            color: var(--primary-color);
            border: 2px solid var(--primary-color);
        }

        .btn-secondary:hover {
            background-color: var(--primary-color);
            color: var(--white);
            transform: translateY(-5px);
            box-shadow: 0 15px 30px rgba(51, 51, 51, 0.15);
        }

        /* Avatar moderno */
        .hero-visual {
            flex: 1;
            display: flex;
            justify-content: center;
            animation: fadeInRight 1s ease;
        }

        .avatar-container {
            position: relative;
            width: 380px;
            height: 380px;
            max-width: 100%;
        }

        .avatar-circle {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            background: linear-gradient(135deg, rgba(51, 51, 51, 0.1) 0%, rgba(85, 85, 85, 0.1) 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
            box-shadow: 0 20px 50px rgba(51, 51, 51, 0.1);
        }

        .avatar-initials {
            font-size: 6rem;
            font-weight: 800;
            background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            z-index: 2;
        }

        .avatar-circle::before {
            content: '';
            position: absolute;
            width: 150%;
            height: 150%;
            background: conic-gradient(
                transparent 0deg,
                transparent 180deg,
                var(--primary-color) 180deg,
                var(--primary-color) 270deg,
                transparent 270deg,
                transparent 360deg
            );
            animation: rotate 10s linear infinite;
        }

        .avatar-circle::after {
            content: '';
            position: absolute;
            inset: 8px;
            border-radius: 50%;
            background-color: var(--light-color);
        }

        .avatar-decoration {
            position: absolute;
            width: 115%;
            height: 115%;
            top: -7.5%;
            left: -7.5%;
            border-radius: 50%;
            border: 20px solid rgba(51, 51, 51, 0.1);
            animation: pulse 4s infinite ease-in-out;
            z-index: -1;
        }

        @keyframes rotate {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }

        /* Indicador de scroll elegante */
        .scroll-indicator {
            position: absolute;
            bottom: 60px;
            left: 50%;
            transform: translateX(-50%);
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 8px;
        }

        .scroll-indicator span {
            display: block;
            width: 3px;
            height: 25px;
            background: linear-gradient(to bottom, var(--primary-color), var(--primary-light));
            border-radius: 10px;
            animation: bounce 2.5s infinite;
            transform-origin: center;
        }

        .scroll-indicator span:nth-child(1) { animation-delay: 0.1s; }
        .scroll-indicator span:nth-child(2) { animation-delay: 0.2s; height: 20px; }
        .scroll-indicator span:nth-child(3) { animation-delay: 0.3s; height: 15px; }

        .scroll-text {
            font-size: 0.9rem;
            color: var(--text-light);
            margin-top: 15px;
            opacity: 0.8;
            animation: fadeInOut 2.5s infinite;
            letter-spacing: 1px;
            text-transform: uppercase;
            font-family: var(--font-secondary);
            font-weight: 500;
        }

        @keyframes fadeInOut {
            0%, 100% { opacity: 0.6; }
            50% { opacity: 1; }
        }

        @keyframes bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-15px); }
        }

        /* Seções com efeito de vidro refinado */
        section {
            padding: 100px 0;
            position: relative;
        }

        .section-header {
            text-align: center;
            margin-bottom: 70px;
            position: relative;
        }

        .section-header h2 {
            font-size: clamp(2rem, 6vw, 3rem);
            margin-bottom: 20px;
            display: inline-block;
            position: relative;
        }

        .section-header h2 span {
            color: var(--primary-color);
            position: relative;
        }

        .section-header h2 span::after {
            content: '';
            position: absolute;
            bottom: 8px;
            left: 0;
            width: 100%;
            height: 10px;
            background-color: rgba(51, 51, 51, 0.2);
            z-index: -1;
            border-radius: 8px;
        }

        .section-divider {
            width: 100px;
            height: 5px;
            background: linear-gradient(90deg, var(--primary-color), var(--accent-color));
            margin: 0 auto;
            border-radius: 3px;
            position: relative;
        }

        .section-divider::after {
            content: '';
            position: absolute;
            width: 40px;
            height: 12px;
            background-color: var(--accent-color);
            bottom: -4px;
            left: 50%;
            transform: translateX(-50%);
            border-radius: 6px;
        }

        /* Seção Sobre elegante */
        .about-content {
            display: flex;
            gap: 60px;
            align-items: center;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 40px;
        }

        .about-text {
            flex: 1;
            background-color: rgba(255, 255, 255, 0.94);
            padding: 50px;
            border-radius: var(--border-radius);
            box-shadow: var(--shadow);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.4);
            transition: var(--transition);
        }

        .about-text:hover {
            transform: translateY(-8px);
            box-shadow: var(--shadow-hover);
        }

        .about-text h3 {
            font-size: 2rem;
            margin-bottom: 25px;
            color: var(--dark-color);
            position: relative;
            display: inline-block;
        }

        .about-text h3::after {
            content: '';
            position: absolute;
            bottom: 8px;
            left: 0;
            width: 100%;
            height: 10px;
            background-color: rgba(51, 51, 51, 0.2);
            z-index: -1;
            border-radius: 8px;
        }

        .about-text p {
            margin-bottom: 30px;
            color: var(--text-light);
            font-size: 1.1rem;
            line-height: 1.7;
        }

        .personal-info {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 25px;
            margin-top: 40px;
        }

        .info-item {
            display: flex;
            gap: 15px;
            align-items: center;
        }

        .info-label {
            font-weight: 600;
            color: var(--dark-color);
            min-width: 120px;
            font-size: 1rem;
        }

        .info-value {
            color: var(--text-light);
            background-color: rgba(51, 51, 51, 0.05);
            padding: 10px 20px;
            border-radius: 50px;
            flex: 1;
            font-size: 1rem;
        }

        .about-stats {
            flex: 1;
            display: grid;
            gap: 40px;
        }

        .stat-card {
            background-color: rgba(255, 255, 255, 0.94);
            padding: 40px 30px;
            border-radius: var(--border-radius);
            box-shadow: var(--shadow);
            transition: var(--transition);
            text-align: center;
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.4);
        }

        .stat-card:hover {
            transform: translateY(-10px);
            box-shadow: var(--shadow-hover);
        }

        .stat-icon {
            width: 80px;
            height: 80px;
            background: linear-gradient(135deg, rgba(51, 51, 51, 0.1) 0%, rgba(85, 85, 85, 0.1) 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 25px;
            font-size: 2rem;
            color: var(--primary-color);
            border: 1px solid rgba(51, 51, 51, 0.2);
            transition: var(--transition);
        }

        .stat-card:hover .stat-icon {
            transform: rotate(15deg) scale(1.1);
            background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
            color: var(--white);
        }

        .stat-card h3 {
            margin-bottom: 20px;
            font-size: 1.5rem;
            color: var(--dark-color);
        }

        .stat-card p {
            color: var(--text-light);
            font-size: 1rem;
            line-height: 1.6;
        }

        /* Seção Habilidades */
        .skills-section {
            background-color: rgba(51, 51, 51, 0.03);
        }

        .skills-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 40px;
        }

        .skills-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
        }

        .skill-card {
            background-color: rgba(255, 255, 255, 0.94);
            padding: 35px;
            border-radius: var(--border-radius);
            box-shadow: var(--shadow);
            transition: var(--transition);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.4);
        }

        .skill-card:hover {
            transform: translateY(-8px);
            box-shadow: var(--shadow-hover);
        }

        .skill-card h3 {
            margin-bottom: 20px;
            color: var(--primary-color);
            font-size: 1.5rem;
            position: relative;
            display: inline-block;
        }

        .skill-card h3::after {
            content: '';
            position: absolute;
            bottom: 6px;
            left: 0;
            width: 100%;
            height: 8px;
            background-color: rgba(51, 51, 51, 0.2);
            z-index: -1;
            border-radius: 6px;
        }

        .skill-card ul {
            list-style-type: none;
        }

        .skill-card li {
            margin-bottom: 12px;
            color: var(--text-light);
            font-size: 1rem;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .skill-card li i {
            color: var(--primary-color);
        }

        /* Seção Linguagens de Programação */
        .languages-section {
            background-color: rgba(51, 51, 51, 0.03);
        }

        .languages-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 40px;
        }

        .languages-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
        }

        .language-card {
            background-color: rgba(255, 255, 255, 0.94);
            padding: 35px;
            border-radius: var(--border-radius);
            box-shadow: var(--shadow);
            transition: var(--transition);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.4);
        }

        .language-card:hover {
            transform: translateY(-8px);
            box-shadow: var(--shadow-hover);
        }

        .language-card h3 {
            margin-bottom: 20px;
            color: var(--primary-color);
            font-size: 1.5rem;
            position: relative;
            display: inline-block;
        }

        .language-card h3::after {
            content: '';
            position: absolute;
            bottom: 6px;
            left: 0;
            width: 100%;
            height: 8px;
            background-color: rgba(51, 51, 51, 0.2);
            z-index: -1;
            border-radius: 6px;
        }

        .language-card ul {
            list-style-type: none;
        }

        .language-card li {
            margin-bottom: 12px;
            color: var(--text-light);
            font-size: 1rem;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .language-card li i {
            color: var(--primary-color);
        }

        /* Seção Contato elegante */
        .contact-section {
            background-color: rgba(51, 51, 51, 0.03);
        }

        .contact-container {
            display: flex;
            gap: 60px;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 40px;
        }

        .contact-info {
            flex: 1;
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 30px;
        }

        .contact-card {
            background-color: rgba(255, 255, 255, 0.94);
            padding: 40px 30px;
            border-radius: var(--border-radius);
            box-shadow: var(--shadow);
            transition: var(--transition);
            text-align: center;
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.4);
        }

        .contact-card:hover {
            transform: translateY(-8px);
            box-shadow: var(--shadow-hover);
        }

        .contact-icon {
            width: 80px;
            height: 80px;
            background: linear-gradient(135deg, rgba(51, 51, 51, 0.1) 0%, rgba(85, 85, 85, 0.1) 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 25px;
            font-size: 2rem;
            color: var(--primary-color);
            border: 1px solid rgba(51, 51, 51, 0.2);
            transition: var(--transition);
        }

        .contact-card:hover .contact-icon {
            transform: scale(1.1);
            background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
            color: var(--white);
        }

        .contact-card h3 {
            margin-bottom: 20px;
            font-size: 1.5rem;
            color: var(--dark-color);
        }

        .contact-card p {
            color: var(--text-light);
            font-size: 1rem;
        }

        .contact-form {
            flex: 1;
            background-color: rgba(255, 255, 255, 0.94);
            padding: 50px;
            border-radius: var(--border-radius);
            box-shadow: var(--shadow);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.4);
            transition: var(--transition);
        }

        .contact-form:hover {
            transform: translateY(-5px);
            box-shadow: var(--shadow-hover);
        }

        .form-group {
            margin-bottom: 25px;
        }

        .form-group input,
        .form-group textarea {
            width: 100%;
            padding: 16px 25px;
            border: 1px solid rgba(0, 0, 0, 0.1);
            border-radius: var(--border-radius);
            font-family: var(--font-secondary);
            font-size: 1rem;
            transition: var(--transition);
            background-color: rgba(255, 255, 255, 0.8);
        }

        .form-group input:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgba(51, 51, 51, 0.2);
            background-color: var(--white);
        }

        .btn-submit {
            background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
            color: var(--white);
            border: none;
            padding: 18px 35px;
            border-radius: 50px;
            font-weight: 600;
            font-size: 1rem;
            cursor: pointer;
            transition: var(--transition);
            display: inline-flex;
            align-items: center;
            gap: 12px;
            box-shadow: 0 15px 30px rgba(51, 51, 51, 0.2);
            width: 100%;
            justify-content: center;
            letter-spacing: 0.5px;
        }

        .btn-submit:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 40px rgba(51, 51, 51, 0.3);
            background: linear-gradient(135deg, var(--secondary-color), var(--primary-color));
        }

        /* Rodapé estiloso */
        .main-footer {
            background-color: var(--dark-color);
            color: var(--white);
            padding: 80px 0 0;
            position: relative;
        }

        .footer-content {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 40px;
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 60px;
        }

        .footer-logo {
            display: flex;
            align-items: center;
            gap: 20px;
            margin-bottom: 25px;
        }

        .footer-logo span {
            width: 60px;
            height: 60px;
            background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
            color: var(--white);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.8rem;
            font-weight: 700;
            box-shadow: 0 10px 30px rgba(51, 51, 51, 0.25);
        }

        .footer-logo h3 {
            font-size: 1.8rem;
            font-weight: 700;
        }

        .footer-about p {
            color: rgba(255, 255, 255, 0.7);
            margin-bottom: 25px;
            line-height: 1.7;
            font-size: 1rem;
        }

        .footer-social {
            display: flex;
            gap: 20px;
            margin-top: 25px;
        }

        .footer-social a {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            background-color: rgba(255, 255, 255, 0.1);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.3rem;
            transition: var(--transition);
        }

        .footer-social a:hover {
            background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
            transform: translateY(-5px);
            box-shadow: 0 10px 30px rgba(51, 51, 51, 0.25);
        }

        .footer-links h3 {
            font-size: 1.5rem;
            margin-bottom: 25px;
            position: relative;
            display: inline-block;
        }

        .footer-links h3::after {
            content: '';
            position: absolute;
            bottom: 6px;
            left: 0;
            width: 100%;
            height: 8px;
            background-color: rgba(85, 85, 85, 0.3);
            z-index: -1;
            border-radius: 6px;
        }

        .footer-links ul {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .footer-links a {
            transition: var(--transition);
            display: flex;
            align-items: center;
            gap: 12px;
            color: rgba(255, 255, 255, 0.7);
            font-size: 1rem;
        }

        .footer-links a i {
            color: var(--primary-light);
            transition: var(--transition);
        }

        .footer-links a:hover {
            color: var(--white);
            transform: translateX(8px);
        }

        .footer-links a:hover i {
            color: var(--accent-color);
        }

        .footer-bottom {
            text-align: center;
            padding: 30px;
            margin-top: 80px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            font-size: 0.9rem;
            color: rgba(255, 255, 255, 0.5);
        }

        /* Animações */
        @keyframes fadeInLeft {
            from {
                opacity: 0;
                transform: translateX(-80px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        @keyframes fadeInRight {
            from {
                opacity: 0;
                transform: translateX(80px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        @keyframes pulse {
            0% {
                transform: scale(1);
                opacity: 1;
            }
            50% {
                transform: scale(1.03);
                opacity: 0.8;
            }
            100% {
                transform: scale(1);
                opacity: 1;
            }
        }

        /* Responsividade */
        @media (max-width: 1200px) {
            .hero-content,
            .about-content,
            .contact-container,
            .skills-container,
            .languages-container {
                padding: 0 50px;
            }
            
            .footer-content {
                padding: 0 50px;
            }
            
            .avatar-container {
                width: 340px;
                height: 340px;
            }
        }

        @media (max-width: 992px) {
            .hero-content {
                flex-direction: column;
                text-align: center;
                gap: 80px;
            }
            
            .hero-buttons {
                justify-content: center;
            }
            
            .about-content {
                flex-direction: column;
                gap: 60px;
            }
            
            .contact-container {
                flex-direction: column;
                gap: 60px;
            }
            
            .footer-content {
                grid-template-columns: 1fr 1fr;
                gap: 50px;
            }
            
            .avatar-container {
                width: 320px;
                height: 320px;
            }
            
            .floating-nav {
                right: 25px;
                padding: 18px 10px;
            }
            
            .floating-nav li a {
                width: 45px;
                height: 45px;
                font-size: 1rem;
            }
        }

        @media (max-width: 768px) {
            section {
                padding: 80px 0;
            }
            
            .hero-section {
                padding: 80px 0;
            }
            
            .hero-content,
            .about-content,
            .contact-container,
            .skills-container,
            .languages-container {
                padding: 0 30px;
            }
            
            .name {
                font-size: 2.8rem;
            }
            
            .title {
                font-size: 1.4rem;
            }
            
            .personal-info {
                grid-template-columns: 1fr;
            }
            
            .skills-grid,
            .languages-grid {
                grid-template-columns: 1fr;
            }
            
            .footer-content {
                grid-template-columns: 1fr;
                gap: 40px;
            }
            
            .avatar-container {
                width: 280px;
                height: 280px;
            }
            
            .floating-nav {
                right: 20px;
                padding: 15px 8px;
            }
            
            .section-header {
                margin-bottom: 50px;
            }
            
            .about-text,
            .contact-form {
                padding: 40px;
            }
        }

        @media (max-width: 576px) {
            .hero-content,
            .about-content,
            .contact-container,
            .skills-container,
            .languages-container {
                padding: 0 20px;
            }
            
            .name {
                font-size: 2.5rem;
            }
            
            .title {
                font-size: 1.3rem;
            }
            
            .hero-buttons {
                flex-direction: column;
                gap: 15px;
            }
            
            .btn-main, .btn-secondary {
                width: 100%;
                justify-content: center;
                padding: 14px 24px;
            }
            
            .avatar-container {
                width: 250px;
                height: 250px;
            }
            
            .floating-nav {
                bottom: 20px;
                top: auto;
                right: 50%;
                transform: translateX(50%);
                flex-direction: row;
                padding: 12px 20px;
            }
            
            .floating-nav ul {
                flex-direction: row;
                gap: 15px;
            }
            
            .contact-info {
                grid-template-columns: 1fr;
            }
            
            .section-header h2 {
                font-size: 2.2rem;
            }
            
            .about-text,
            .contact-form {
                padding: 30px;
            }
            
            .footer-content {
                padding: 0 20px;
            }
            
            .footer-logo span {
                width: 50px;
                height: 50px;
                font-size: 1.5rem;
            }
            
            .footer-logo h3 {
                font-size: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <div class="gradient-bg"></div>
    
    <nav class="floating-nav">
        <ul>
            <li><a href="#home" class="nav-link active"><i class="fas fa-home"></i></a></li>
            <li><a href="#about" class="nav-link"><i class="fas fa-user"></i></a></li>
            <li><a href="#skills" class="nav-link"><i class="fas fa-tasks"></i></a></li>
            <li><a href="#languages" class="nav-link"><i class="fas fa-code"></i></a></li>
            <li><a href="#contact" class="nav-link"><i class="fas fa-envelope"></i></a></li>
        </ul>
    </nav>

    <main>
        <section id="home" class="hero-section">
            <div class="hero-content">
                <div class="hero-text">
                    <h4 class="greeting">Olá, eu sou</h4>
                    <h1 class="name">Gabriel <span>da Silva Lima</span></h1>
                    <h2 class="title">Jovem aprendiz em busca de oportunidades</h2>
                    <div class="hero-buttons">
                        <a href="#skills" class="btn-main"><i class="fas fa-tasks"></i> Minhas Habilidades</a>
                        <a href="#about" class="btn-secondary"><i class="fas fa-user"></i> Sobre Mim</a>
                    </div>
                </div>
                <div class="hero-visual">
                    <div class="avatar-container">
                        <div class="avatar-circle">
                            <div class="avatar-initials">GL</div>
                        </div>
                        <div class="avatar-decoration"></div>
                    </div>
                </div>
            </div>
            <div class="scroll-indicator">
                <span></span>
                <span></span>
                <span></span>
                <p class="scroll-text">Role para baixo</p>
            </div>
        </section>

        <section id="about">
            <div class="section-header">
                <h2>Sobre <span>Mim</span></h2>
                <div class="section-divider"></div>
            </div>
            <div class="about-content">
                <div class="about-text">
                    <h3>Quem sou eu</h3>
                    <p>Sou Gabriel da Silva Lima, um jovem aprendiz em busca de oportunidades para desenvolver minhas habilidades profissionais. Tenho boa comunicação, interesse em aprender e sou comprometido com horários.</p>
                    
                    <div class="personal-info">
                        <div class="info-item">
                            <span class="info-label">Nome:</span>
                            <span class="info-value">Gabriel da Silva Lima</span>
                        </div>
                        <div class="info-item">
                            <span class="info-label">E-mail:</span>
                            <span class="info-value">gabrielsilvalimask8@gmail.com</span>
                        </div>
                        <div class="info-item">
                            <span class="info-label">Telefone:</span>
                            <span class="info-value">(11) 95605-9218</span>
                        </div>
                        <div class="info-item">
                            <span class="info-label">Objetivo:</span>
                            <span class="info-value">Atuar como jovem aprendiz</span>
                        </div>
                    </div>
                </div>
                <div class="about-stats">
                    <div class="stat-card">
                        <div class="stat-icon">
                            <i class="fas fa-graduation-cap"></i>
                        </div>
                        <h3>Formação</h3>
                        <p>Estudante em busca de oportunidades para desenvolver habilidades profissionais como jovem aprendiz.</p>
                    </div>
                    <div class="stat-card">
                        <div class="stat-icon">
                            <i class="fas fa-bullseye"></i>
                        </div>
                        <h3>Objetivo Profissional</h3>
                        <p>Busco minha primeira experiência profissional como jovem aprendiz, onde possa aplicar e desenvolver minhas habilidades.</p>
                    </div>
                </div>
            </div>
        </section>

        <section id="skills" class="skills-section">
            <div class="section-header">
                <h2>Minhas <span>Habilidades</span></h2>
                <div class="section-divider"></div>
            </div>
            <div class="skills-container">
                <div class="skills-grid">
                    <div class="skill-card">
                        <h3>Qualificação Profissional</h3>
                        <ul>
                            <li><i class="fas fa-check"></i> Boa comunicação</li>
                            <li><i class="fas fa-check"></i> Interesse em aprender</li>
                            <li><i class="fas fa-check"></i> Comprometimento com horários</li>
                        </ul>
                    </div>
                    <div class="skill-card">
                        <h3>Cursos Adicionais</h3>
                        <ul>
                            <li><i class="fas fa-check"></i> 2024 - Informática básica (Sendi)</li>
                        </ul>
                    </div>
                </div>
            </div>
        </section>

        <section id="languages" class="languages-section">
            <div class="section-header">
                <h2>Linguagens de <span>Programação</span></h2>
                <div class="section-divider"></div>
            </div>
            <div class="languages-container">
                <div class="languages-grid">
                    <div class="language-card">
                        <h3>Conhecimentos Técnicos</h3>
                        <ul>
                            <li><i class="fab fa-js"></i> JavaScript (nível básico)</li>
                            <li><i class="fab fa-html5"></i> HTML (nível intermediário)</li>
                            <li><i class="fab fa-css3-alt"></i> CSS (nível intermediário)</li>
                        </ul>
                    </div>
                </div>
            </div>
        </section>

        <section id="contact" class="contact-section">
            <div class="section-header">
                <h2>Entre em <span>Contato</span></h2>
                <div class="section-divider"></div>
            </div>
            <div class="contact-container">
                <div class="contact-info">
                    <div class="contact-card">
                        <div class="contact-icon">
                            <i class="fas fa-envelope"></i>
                        </div>
                        <h3>E-mail</h3>
                        <p>gabrielsilvalimask8@gmail.com</p>
                    </div>
                    <div class="contact-card">
                        <div class="contact-icon">
                            <i class="fas fa-phone"></i>
                        </div>
                        <h3>Telefone</h3>
                        <p>(11) 95605-9218</p>
                    </div>
                </div>
                <form class="contact-form">
                    <div class="form-group">
                        <input type="text" placeholder="Seu Nome" required>
                    </div>
                    <div class="form-group">
                        <input type="email" placeholder="Seu E-mail" required>
                    </div>
                    <div class="form-group">
                        <input type="text" placeholder="Assunto">
                    </div>
                    <div class="form-group">
                        <textarea rows="5" placeholder="Sua Mensagem" required></textarea>
                    </div>
                    <button type="submit" class="btn-submit">
                        <i class="fas fa-paper-plane"></i> Enviar Mensagem
                    </button>
                </form>
            </div>
        </section>
    </main>

    <footer class="main-footer">
        <div class="footer-content">
            <div class="footer-about">
                <div class="footer-logo">
                    <span>GL</span>
                    <h3>Gabriel Lima</h3>
                </div>
                <p>Jovem aprendiz em busca de oportunidades para aplicar minhas habilidades e crescer profissionalmente.</p>
                <div class="footer-social">
                    <a href="#"><i class="fab fa-linkedin-in"></i></a>
                    <a href="#"><i class="fab fa-instagram"></i></a>
                    <a href="#"><i class="fab fa-whatsapp"></i></a>
                </div>
            </div>
            <div class="footer-links">
                <h3>Links Rápidos</h3>
                <ul>
                    <li><a href="#home"><i class="fas fa-chevron-right"></i> Início</a></li>
                    <li><a href="#about"><i class="fas fa-chevron-right"></i> Sobre Mim</a></li>
                    <li><a href="#skills"><i class="fas fa-chevron-right"></i> Habilidades</a></li>
                    <li><a href="#languages"><i class="fas fa-chevron-right"></i> Linguagens</a></li>
                    <li><a href="#contact"><i class="fas fa-chevron-right"></i> Contato</a></li>
                </ul>
            </div>
            <div class="footer-links">
                <h3>Informações</h3>
                <ul>
                    <li><a href="#"><i class="fas fa-chevron-right"></i> Qualificação Profissional</a></li>
                    <li><a href="#"><i class="fas fa-chevron-right"></i> Cursos Adicionais</a></li>
                    <li><a href="#"><i class="fas fa-chevron-right"></i> Objetivo Profissional</a></li>
                </ul>
            </div>
        </div>
        <div class="footer-bottom">
            <p>&copy; 2024 Gabriel da Silva Lima. Todos os direitos reservados.</p>
        </div>
    </footer>

    <script>
        // Ativar links da navegação flutuante conforme rolagem
        const sections = document.querySelectorAll('section');
        const navLinks = document.querySelectorAll('.nav-link');

        window.addEventListener('scroll', () => {
            let current = '';
            
            sections.forEach(section => {
                const sectionTop = section.offsetTop;
                const sectionHeight = section.clientHeight;
                
                if (pageYOffset >= (sectionTop - 200)) {
                    current = section.getAttribute('id');
                }
            });
            
            navLinks.forEach(link => {
                link.classList.remove('active');
                if (link.getAttribute('href') === `#${current}`) {
                    link.classList.add('active');
                }
            });
        });

        // Suavizar rolagem para links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function(e) {
                e.preventDefault();
                
                document.querySelector(this.getAttribute('href')).scrollIntoView({
                    behavior: 'smooth'
                });
            });
        });
    </script>
</body>
</html>
